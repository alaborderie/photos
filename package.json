{
  "name": "photos",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-scripts": "^2.1.5"
  },
  "scripts": {
    "prestart": "yarn build --prod",
    "start": "node api/index.js",
    "prebuild": "rm -rf api/public",
    "build": "react-scripts build",
    "postbuild": "mv build api/public",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "serve": "react-scripts start"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
